# Research Ledger

Every hypothesis this project has tested, what was actually measured, and the
verdict. The point of this file is that no one re-runs a settled question or
re-adopts a rejected idea because the reasoning was never written down.

> **2026-09-03 — portfolio_v4 / the whole M15 approach is DEAD.**
> 4-year holdout (`scripts/archive/rohith2_deadend/proximity_holdout.py`): PF
> **0.965**, net −$96, min balance $10. Per-6-month-window
> (`edge_by_period.py`): PF swings 0.75–1.48, drawdowns 21–99.8%; the selection
> window is the single best 6-month period in 4+ years. Every rohith-2
> refinement (7 exit variants, 6 gate variants, SMC filters) was worse or
> marginal. **Verdict: no out-of-sample edge; overfit to the selection window.**
> Two independent efforts now agree (see HYP-020 random-entry control).
> Replacement: `docs/research/D1_SYSTEM_PLAN.md`. Faults: `SYSTEM_FAULTS.md`.

**Rules for this file**

- One row per hypothesis, with an ID that never gets reused.
- A verdict must cite a run directory under `research/runs/` or it is not a verdict.
- "Insufficient evidence" is a valid, common, and honest outcome. It is not a
  polite way of saying "probably fine".
- If a result is later contradicted, do not edit the old row. Add a new one and
  link back. The history of what we believed is part of the evidence.

**Standing measurement setup** unless a row says otherwise:

| | |
|---|---|
| Instrument | XAUUSDm (Exness-MT5Trial11, server clock UTC+0.00) |
| Data | Broker M15 via MT5, hash `293e23125651c4a5` |
| Development window | 2025-04-03 → 2026-08-29 (17 months) — **burned, cannot validate** |
| Locked holdout | 2022-06-07 → 2025-04-02 (2y10m) — **untouched** |
| Costs | `realistic` = observed per-bar spread + 20 points adverse slippage |
| Sizing | Fixed 0.01 lots for edge measurement; live sizing for survivability |
| Engine | `src/backtesting/` — calls the live strategy and risk modules |

---

## Settled

| ID | Hypothesis | Verdict | Evidence |
|----|-----------|---------|----------|
| **HYP-001** | Previous backtest results describe the deployed system. | **REJECTED** | The old engine booked a fixed ±$5 per trade, ignored the realised exit price, applied no costs, and modelled a fixed 3.0×ATR target while live ran a 0.3×ATR trailing stop. It measured a system that was never deployed. |
| **HYP-002** | Yahoo `GC=F` is an acceptable proxy for XAUUSDm. | **REJECTED** | COMEX futures: different price level, contract rolls, CME session calendar with a daily halt, and a 60-day intraday cap. `XAUUSD=X` carries no volume at all, so the volume filter — one of MorningMomentum's four gates — was untestable. |
| **HYP-003** | The strategies are profitable before costs. | **SUPPORTED** | PF 1.072, +$564.96 over 1,672 trades, frictionless. `20260831-161102_cost_sensitivity` |
| **HYP-004** | The strategies are profitable after real costs. | **NOT ESTABLISHED** | Observed spread → PF 1.023. Plus slippage → 1.010 (+$80.25). Stressed → 0.954. Bootstrap P(expectancy≤0) = 45% at realistic costs. **86% of gross edge is consumed by spread and slippage.** `20260831-161102_cost_sensitivity` |
| **HYP-005** | The 0.3×ATR trailing stop protects profit. | **REJECTED — it is the most harmful component** | Removing it: PF 1.010→1.044, net +$80→+$424, P(no edge) 45%→24%. Win rate falls 57%→35% while payoff rises 0.73→1.90. Exit tally under HEAD: **1,686 SL vs 2 TP**. Average MFE +0.69R against average realised +0.005R. `20260831-160243_ablation_long` |
| **HYP-006** | MorningMomentum is the core profitable strategy ("A grade, 83% win rate, IMMUTABLE"). | **REJECTED** | PF 0.87 over 674 trades / 17 months. Worse under every variant tested (PF 0.83 with trailing removed). The 83% figure traces to `src/strategies/archive/README.md` citing an "Aug 25, 2026 forensic audit" with no backing run in the repo — unverifiable. **Re-confirmed 2026-09-01: tier-1 screen excluded this hypothesis; tier-2 validation unnecessary.** `20260831-160243_ablation_long` |
| **HYP-007** | EMAPullback should stay disabled (30-day backtest: −$111.81, 27.6% WR). | **REJECTED** | PF 1.09 over 985 trades, +$456.02 — the only strategy of the three with a positive signal. The 30-day rejection was computed on the invalid engine. `20260831-160243_ablation_long` |
| **HYP-008** | The consolidation exit is an active risk control. | **REJECTED — it has never fired** | Disabling it produces a byte-identical result across 1,688 trades: same count, win rate, net, drawdown. It is also position-independent and would close every open position at once if it ever did fire. `20260831-160243_ablation_long` |
| **HYP-009** | The EMA-200 warm-up truncation (250-bar window) materially distorts results. | **REJECTED** | Widening the window to 1,200 bars: PF 1.010 → 1.010, net +$80.25 → +$87.75. A correctness defect, not a P&L defect. `20260831-160243_ablation_long` |
| **HYP-010** | Per-candle deduplication improves profitability. | **REJECTED — but keep it anyway** | Dedup: PF 1.010→0.982, net +$80→−$139. Duplicates are symmetric; they double winners and losers alike. The case for dedup is exposure and variance control, not return. It caused the −$55.58 doubled loss on 2026-08-25. `20260831-160243_ablation_long` |
| **HYP-011** | The D1 bias gate is redundant over-filtering. | **REJECTED — the gate helps** | Removing it: 1,688 → 2,927 trades, PF 1.010 → 0.990. It is filtering real losers. `20260831-160243_ablation_long` |
| **HYP-012** | London Open (11:30–15:30 IST) is the highest-value session. | **REJECTED** | LONDON_OPEN PF 0.94 (−$175, n=641); LONDON_NY_OVERLAP PF 0.80 (−$264, n=279); NY_MORNING PF 1.01 (+$39, n=718). The architecture is built around the worst window. `20260831-160243_ablation_long` |
| **HYP-013** | Asia/overnight is the most profitable session (PF 4.84). | **REJECTED — artifact** | Those 50 trades are pyramid add-ons at 00:00–08:00 IST, spread 1–7 per hour. `_check_pyramiding` has no session check, so "London only" strategies add exposure overnight. Not a session result. |
| **HYP-014** | The broker server clock is offset from UTC, skewing session gates. | **REJECTED** | Measured against the live terminal: Exness-MT5Trial11 runs at **UTC+0.00**. The strategies' timestamp conversion is correct. Withdraws audit finding HIGH-5. |
| **HYP-015** | The 0.01 lot floor protects a small account from 15% sizing. | **REJECTED — it protects nothing** | At balance $105.74 and ATR $10.56, a 1.5×ATR stop sizes to exactly 0.0100 lots risking $15.84 = **15.0%**. The 2.0×ATR Asia stop clamps up and risks **20.0%**. The floor is not a cap; it is the minimum. |
| **HYP-016** | Repairing the known defects makes the system survivable at the current balance. | **REJECTED** | At $105.74 over the M1-fidelity window: HEAD → −100.0% in 16 days; REPAIRED → −100.7% in 47 days. Repairs cut max drawdown $306.62 → $114.53 and tripled survival time, but do not create an edge. `20260831-162259_dup_cost` |
| **HYP-017** | Tighter take-profits raise the win rate enough to compensate. | **REJECTED** | Monotonic and in the opposite direction. TP 1.5×ATR: PF 0.989. TP 2.0: 0.996. TP 3.0: 1.076. TP 5.0: 1.194. Every exit-tightening change made during the live week moved along this gradient the wrong way. `20260831-171130_exit_research` |
| **HYP-018** | Some trailing configuration beats no trailing. | **REJECTED** | Best of 13 trail cells is activation 2.0 / distance 0.3 at PF 1.027 — below every no-trail variant from TP 3.0 upward. The live setting (0.7 / 0.3) is near the worst of the grid at PF 0.982. `20260831-171130_exit_research` |
| **HYP-019** | The "wider TP is better" result is gold's uptrend in disguise (+41.5% over the window). | **REJECTED** | Shorts outperform longs at every TP from 5.0 to 12.0 — e.g. at TP 5.0, SHORT PF 1.324 (+$1,457) vs LONG PF 1.090 (+$504). A pure long-drift artifact cannot produce that. `20260831-171734_exit_extend` |

---

## Open

| ID | Hypothesis | Status | Next step |
|----|-----------|--------|-----------|
| **HYP-020** | The entry signals contribute anything at all beyond the exit geometry. | **TESTING** | Random-entry control: coin-flip entries, identical sessions/stops/exits/costs/trade-count. If random scores like the strategies, there is no entry edge. `scripts/random_entry_control.py` |
| **HYP-021** | The wide-TP result is a deployable edge rather than a fat-tailed lottery. | **DOUBTFUL** | TP 20×ATR: 9.85% win rate, payoff 12.5, only 9/17 profitable months, swinging −$836 (2026-02) to +$1,196 (2026-03). The result rests on ~65 winning trades out of 660. Needs outlier-dependence and drawdown-duration analysis before it is taken seriously. |
| **HYP-022** | The locked holdout can validate a candidate configuration. | **PARTIALLY BLOCKED** | Gold rose +68.3% across the holdout and +41.5% across development. **There is no bear-market XAUUSDm data on this terminal.** A holdout that is also a bull market cannot falsify a trend-dependent result. Regime validation needs an external data source. |
| **HYP-023** | AsianSweep has an edge. | **INSUFFICIENT EVIDENCE** | 29 trades in 17 months, PF 1.70. Too few to distinguish from noise, and its $5–$35 Asian-range band is a fixed-dollar threshold set at a much lower gold price. |
| **HYP-024** | The account can trade XAUUSDm safely at any risk setting. | **REJECTED, pending your decision** | The 0.01 lot minimum on a 100oz contract fixes minimum risk at the stop distance in dollars: $15.84 at current ATR. 2% risk requires ~$792 of balance; 1% requires ~$1,584. At the $200 milestone the minimum is still 7.9%. No smaller gold contract exists on this server (all six XAU symbols checked). |
| **HYP-025** | EMA7/21/200 + Parabolic SAR flip + ADX>25 + ChoCh + volume (7-condition gate) produces an edge. | **REJECTED** | Tier-1 screen: 427 trades, exp +0.0008R, edge_z +1.09, PF undefined (small expectancy). Tier-2 engine (M1 sub-bar fidelity, realistic costs): account $105.74 → ~$0 over holdout window, daily loss limit repeatedly breached. The selectivity (7 gates) reduced signal frequency but not drawdown. 0.01-lot survivability failed. `2026-09-01, scripts/tier2_ema_sar_simple` |
| **HYP-026** | EMA200 filter + Parabolic SAR flip (simple 2-condition gate) produces an edge. | **REJECTED** | Tier-1 screen: 1,451 trades, exp +0.0034R, edge_z +1.88, ranked #11 of 120 candidates — appeared promising. Tier-2 engine: identical failure mode to HYP-025. Account $105.74 → ~$0, daily shutdowns cascade as balance shrinks. No separation from random-entry baseline under execution fidelity. 0.01-lot survivability failed. `2026-09-01, scripts/tier2_ema_sar_simple` |
| **HYP-027** | EMA_STACK survives at the real operating constraint: $105.74 starting balance, fixed 0.01 lots, live 6%-daily-loss breaker on. | **SUPPORTED, with caveats — first candidate to clear this bar** | Every prior EMA_STACK number (incl. the grade-B PF 1.073 holdout figure in the registry) was measured at a **$100,000 synthetic balance with the daily breaker off** — edge isolation, not survivability. Rerun at the real balance: in-sample PF 1.319 (+0.2030R/trade), $105.74 → $3,874.76 over 1,436 trades, max drawdown 32.2% of peak, daily breaker tripped 9/316 days. Holdout PF 1.081 (+0.0675R/trade), $105.74 → $669.65 over 2,661 trades, but the account dipped to **$41.79 (60% underwater) at trade #1094**, and the breaker tripped 82/581 days — a materially rougher ride than in-sample. Neither window ever hit the $5 ruin floor. Caveat carried over from HYP-022: both windows are bull-market gold; no bear-market data exists on this broker to stress it further. Fixed-lot sizing means % risk falls as the balance compounds, so the first weeks at ~$105 are structurally the most dangerous part of any live run — consistent with the holdout's near-60%-drawdown event, which should be read as a preview of what an early losing streak looks like in dollars, not a tail risk that goes away later. `2026-08-31, research/runs/20260831-185921_ema_stack_survivability, scripts/tier2_ema_stack_survivability` |
| **HYP-028** | The top-10 tier-1 candidates by edge_z (excluding the already-settled SAR pair) survive the real $105.74/0.01-lot/breaker-on constraint at least as well as EMA_STACK. | **REJECTED — all 10 net positive, none beats EMA_STACK** | Every candidate (XAU-049, 006, 003, 075, 092, 005, 002, 026, 062, 004) was rerun through the full tier-2 engine at the real balance with the breaker on. All ten finished net positive (PF 1.08–1.30), but every one carries materially worse survivability than plain EMA_STACK: peak drawdowns of 51–88% (vs EMA_STACK's 32.2%) and 25–68 daily-breaker trips over the same window (vs EMA_STACK's 9). Best of the ten: XAU-006 (EMA stack, London+NY, tight 1.5/1.5 exit) at PF 1.166, 53.7% DD, 25 breaches — still clearly worse than EMA_STACK on every axis. `2026-08-31, research/runs/20260831-190849_screen_survivors, scripts/tier2_screen_survivors` |
| **HYP-029** | The gap between XAU-005 (tier-1 EMA-stack candidate, PF 1.245) and live EMA_STACK (PF 1.319) is caused by exit-geometry differences. | **REJECTED — the gap is session width, not exit geometry** | XAU-005 and live EMA_STACK share the identical entry rule and identical 3.0×ATR TP / 1.5×ATR SL. The only difference: XAU-005 was session-gated to London+NY (11:30–21:30 IST) per the tier-1 screen's own labelling of "ALL", while live EMA_STACK has no session gate and trades all 24 hours. Rerunning the same rule and exit with the session gate removed reproduces live EMA_STACK's numbers exactly: 1,436 trades, PF 1.319, $105.74→$3,874.76, 32.2% DD, 9 breaches — an exact match, confirming session width as the explanation. `2026-08-31, research/runs/20260831-203444_modified_variants (MOD-2), scripts/tier2_modified_variants` |
| **HYP-030** | Widening a candidate to 24-hour trading is a generally safe way to improve survivability (per HYP-029). | **REJECTED as a general rule — effect is entry-specific and can be catastrophic or deceptively dangerous** | Two counter-examples in the same run set as HYP-029's confirming case. (a) HH/HL structure (XAU-026, NY-only, PF 1.128, 51.2% DD in the main sweep) widened to 24h collapsed almost immediately: 7 trades, PF 0.000, balance went **negative** (−$9), effectively ruined within the first handful of trades — overnight conditions are specifically hostile to this entry, the opposite of what happened to the EMA ribbon. (b) EMA_STACK's own entry, widened to 24h but kept at XAU-006's tight 1.5/1.5 exit instead of EMA_STACK's native 3.0/1.5, produced a headline result that looks competitive with plain EMA_STACK (2,751 trades, PF 1.221, $105.74→$3,643) but the account dipped to **$10 — one bad trade from the $5 ruin floor** — with a 91.1% peak drawdown, far worse than plain EMA_STACK's 32.2%. The total-profit figure alone would have hidden this near-miss. Conclusion: session width and exit width are not independent levers — EMA_STACK's specific combination (24h + no fixed/tight TP) is not automatically reproducible by changing either one on a different entry, and the resulting near-ruin event is exactly the kind of risk raw PF/net-P&L does not surface. `2026-08-31, research/runs/20260831-203444_modified_variants (MOD-1, MOD-3), scripts/tier2_modified_variants` |
| **HYP-031** | Excluding the Asia/overnight session (02:30–11:30 IST) improves survivability, since thin overnight liquidity is the obvious suspect behind HYP-030's failures. | **REJECTED for the champion configuration; SUPPORTED only as a risk/return trade for an inferior one** | Two arms, same tier-2 setup. (a) **EMA_STACK's native 3.0/1.5 exit**: removing Asia is harmful on *every* axis — trades 1,436→974, PF 1.319→1.221, net +$3,769→+$1,767, max DD 32.2%→79.6%, daily-breaker trips 9→60, min balance $72→$41. Asia hours are actively carrying part of this setup's edge, which also retroactively explains HYP-029 (24h beat London+NY specifically *because* of the Asia contribution, not merely from more trades). (b) **The tight 1.5/1.5 exit**: removing Asia is a genuine safety trade — the near-ruin dip that made the 24h tight variant unrecommendable largely disappears (min balance $10→$66, max DD 91.1%→42.7%) at the cost of roughly half the return (+$3,537→+$1,897, PF 1.221→1.179, breaches 14→17). This is the clearest risk/return trade-off found anywhere in this research: a large reduction in ruin risk purchased with ~half the profit. **Neither no-Asia variant beats plain EMA_STACK on any metric**, so excluding Asia is not a route to a better strategy — only to making an inferior one less dangerous. (c) **HH/HL structure** — the entry that HYP-030 showed was *ruined* by 24h trading (7 trades, balance to −$9): excluding Asia and changing nothing else turns it into **+$1,233 over 1,110 trades, PF 1.135**. The Asia session alone accounts for the entire gap between ruin and profitability for this entry, confirming overnight conditions as the specific mechanism behind HYP-030(a). However it remains unsafe at this account size — min balance $20 and 81.3% max DD, versus its NY-only version's $77 and 51.2% — so it earns 2.5x the profit of NY-only (+$1,233 vs +$491) while being materially likelier to be wiped out on the way. Ranking flips with the question asked: "which made more" favours no-Asia, "which survives $105 of starting capital" favours NY-only. `2026-09-01, research/runs/20260831-*_no_asia, scripts/tier2_no_asia` |

---

## CORRECTIONS ISSUED 2026-09-01 (Phase 0 audit) — READ BEFORE USING ANY FIGURE ABOVE

| ID | Correction | Impact |
|----|-----------|--------|
| **COR-001** | **All tier-2 figures dated 2026-08-31 are inflated.** Those scripts called `load_bars(timeframes=("M15","M5","D1"))`, excluding M1, so the engine fell back to treating whole M15 bars as single bars (`engine.py:537-540`) for 100% of bars. Measured on the window where M1 exists: PF 1.444 without M1 vs **1.160 with M1**; net +$866.61 vs **+$290.97**; 78.6% of trades were ambiguity-resolved vs 0.5%. | **HYP-025 … HYP-031 verdicts stand directionally, but every number in them is an upper bound, not a measurement.** EMA_STACK's headline PF 1.319 / $105.74→$3,874.76 is not trustworthy. Evidence: `scripts/audit_m1_fidelity.py`, `reports/PHASE_0_RESEARCH_AUDIT.md`. |
| **COR-002** | **HYP-015 and HYP-024 are WRONG as stated.** Both concluded the 0.01-lot minimum "forces" 15–20% risk per trade. That was an artifact of assuming a 1.5×ATR stop. The broker's `stops_level = 0` — **no minimum stop distance is imposed** — so risk per trade is a strategy design choice. A $5 stop risks 4.7% of the account; a $3 stop risks 2.8%. | The account-size problem is **less severe than recorded**, but conditional: the tightest *statistically valid* stop is ~$5–6 and only in the ASIA / LATE sessions (measured noise floor). See `reports/ACCOUNT_100_USD_FEASIBILITY.md`. |
| **COR-003** | **Execution-realistic backtesting is permanently capped at ~3.5 months.** The broker serves M1 only from 2026-05-20 regardless of how far back it is requested (verified at 1y/2y/3y). The locked holdout (2022-06-08 → 2025-04-02) has no M1 *or* M5 — it is M15-resolution only. | Multi-year walk-forward at true fidelity (Phase 9) is **impossible**. Long-window studies are ranking tools, not validation. |
| **COR-004** | **Unmodelled costs and constraints found.** Long swap −$0.52/night per 0.01 lot (shorts $0.00) was never in any cost model. Margin caps concurrent positions at **4** ($22.14 per 0.01 lot on $105.74). `sl_atr_mult_override` in `EngineConfig` is declared but wired to nothing. `screener.py::_session_mask` wraparound branch (`ist_hour < b - 24.0`) is never true — overnight sessions silently lose their post-midnight half. | Any multi-day long-hold result is optimistic. Any wraparound-session screen result is invalid. |
| **COR-005** | **Volatility regime shift.** Mean true range is 4.67 across full M15 history vs **10.03** over the last 500 bars. | Historical results were generated at roughly **half** today's dollar risk per ATR-based stop. Prior survivability conclusions are optimistic about present conditions. |

---

| **HYP-033** | Time-of-day-corrected ATR (Phase 6's `time_of_day_atr`) narrows stops in LATE/ASIA per Phase 2's flat-ATR-mis-sizing claim, directly reducing risk%. | **REJECTED as stated -- the correction widened the stop, not narrowed it** | Wired `use_time_of_day_atr` into the engine (opt-in, patches the engine's own `RiskManager` instance only -- `src/core/risk_manager.py` untouched). Tested on `fvg` LATE session: flat ATR gave avg stop $14.02 (13.3% risk); tod-corrected gave avg stop $17.10 (16.2% risk) -- the OPPOSITE direction predicted. Root cause: Phase 2's "1.85x overstated" figure compared flat ATR14 (a backward-looking volatility estimate) against Phase 1's MAE-p50 (a forward-looking realized-noise statistic over a 4h window) -- these measure related but different things, and the comparison conflated them. The tod-corrected run did score marginally better (PF 1.002->1.059, net $1->$25) but on a small sample (n=37 vs 33) and consistent with the already-established "wider stop = fewer premature shakeouts" pattern (HYP shown in squeeze_break widening tests), not because it corrected a genuine mis-sizing in the claimed direction. **Conclusion: the flat-vs-tod-ATR question needs a like-for-like comparison (both against realized MAE, or both as trailing volatility estimates) before either is trusted; Phase 2's original distortion table should be read as "these two things differ," not "the stop is currently wrong by this ratio."** `2026-09-01, scripts/phase3_worker.py + src/backtesting/engine.py use_time_of_day_atr flag` |

| **HYP-034** | Trailing stops help under tonight's tighter stop distances, even though HYP-005/018 rejected them under the old wider-stop system. | **REJECTED -- reproduces the old finding exactly, on the best current candidate** | Direct A/B on `squeeze_break` ASIA (2.0/4.0xATR, tonight's best-validated config): no trailing -> PF 1.741, net +$285, WR 46.9%, MFE capture 33%. With trailing -> PF 0.664, net **-$87**, WR *rises* to 64.7% but avg realized R goes negative (capture -26%) -- trailing converts real winners into net losses by exiting on the pullback before the move completes. Same mechanism as HYP-005: a trailing stop that activates close to entry gets hit by ordinary noise before the trade reaches its potential. Two more A/B pairs confirm it's not a one-off: `ema_stack` LONDON (no trail PF 1.396/+$159 -> trail PF 1.019/+$6, WR rises 49%->65% while capture falls to 0%) and `fvg` ASIA (no trail PF 1.287/+$201 -> trail PF 0.957/**-$21**, same WR-up-but-capture-collapses pattern). **3 for 3 across different sessions and different entry rules -- this is not noise.** User asked directly whether trailing would help ("even if trade were to go wrong we end up with more profit"); measured answer is no, and the failure mode is worse than "smaller profit" -- it consistently trades win-rate for capture, and capture is what pays. `2026-09-01, scripts/trail_ab_test.py` |

| **HYP-035** | User-specified target profile: a strategy risking ~$9-15 per trade with ~$20-30 average wins (high reward:risk, so daily $ target scales cleanly as risk tolerance grows from $20/day toward $30-40/day later). | **FOUND -- `hhll_structure` (HH/HL swing structure entry), LATE session (21:30-24:00 IST), 0.75x/3.0xATR** | n=57, WR 26.3%, PF **1.467**, avg risk **$7.19**, avg win **$29.51**, reward:risk **4.11:1**. Net +$140.86 over the window, min balance $84.95 (never near ruin), end $246.60. Verified independently (fresh process, real trade records with timestamps/fill reasons shown). Caveat: n=57 is above the ~30-trade floor but still a single ~3.5-month window (the only M1-covered period) -- not yet holdout-validated. Low win rate (26%) means real losing streaks will happen; at 4:1 R:R the breakeven WR is only ~24%, so the margin over breakeven is thin in win-rate terms even though PF is healthy. `2026-09-01, scripts/phase3_worker.py (candidate hhll_structure|LATE|SL0.75|TP3.0), independently re-verified` |

| **HYP-036** | A real portfolio -- each of the 3 best per-session strategies at its OWN validated config, sharing one account and the real 6% daily breaker -- beats any single strategy toward the $20/day goal. | **SUPPORTED -- best combined result of the night** | Legs: `squeeze_break` ASIA (2.0/4.0xATR), `ema_stack` LONDON (2.0/3.0xATR), `fvg` NY (0.5/1.5xATR), each run isolated for its own trade list then replayed chronologically on one shared account with the real daily-loss-breaker logic applied manually (the engine's own multi-strategy path doesn't yet support per-strategy TP/SL, so a naive shared-config run understated this -- see HYP note below). Result: 241 trades kept (10 blocked by the shared breaker), WR 39.4%, PF **1.437**, net **+$569.34** over the ~100-day window, min balance **$106 (never dropped below the $105.74 start)**, max DD 22.1%, breaches on 9/64 trading days. **≈$5.69/day** -- the strongest validated combined figure found tonight, ~4.7x better than the first (v2-library-only) combined estimate of $1.21/day. Still ~3.5x short of $20/day. `2026-09-01, scripts/portfolio_merge_honest.py` |
| **HYP-037** | Naive single-shared-config portfolio testing (one TP/SL applied to all legs at once) is a valid way to test multi-strategy interaction. | **REJECTED** | `scripts/portfolio_combo_test.py` forced all 3 legs through one compromise config (tp=2.5/sl=1.2) since `EngineConfig` is global per run. Result: `ema_stack` LONDON alone LOST money (-$41) under that compromise despite being profitable at its own real config (+$197 at 2.0/3.0). The compromise config understated the true portfolio by testing legs outside their validated parameters. HYP-036's manual chronological replay is the correct method until the engine supports per-strategy configs in one run -- a real gap worth fixing (`Optional[Dict[str,EngineConfig]]` keyed by strategy name would do it). `2026-09-01` |
| **HYP-038** | Pushing `squeeze_break` ASIA's stop/target wider than 2.0/4.0xATR continues improving results. | **PARTIALLY SUPPORTED, non-monotonic** | Swept SL 2.0/3.0/4.0xATR (~$15/$22/$29) x proportional TP. Results: $15->PF 1.741/net $285; $22->PF 1.019/net $11 (a real dip, not a typo); $29->PF **1.722**/net **$432** (best single-leg net of the night, avg win $54); $22-with-9x-target->PF 1.515/net $309 (avg win $65, highest average win found, but fewer big winners than the $29 config makes up for it). **Not monotonic** -- $22 is a trough between two peaks, meaning "just widen more" is not a safe rule; each step needs its own test, exactly as the project's own methodology insists. `2026-09-01, scripts/widen_test (ad hoc)` |

| **HYP-039** | The $20/day target is roughly 3x short of what's been validated (~$7/day), a gap closeable by continued strategy search. | **REJECTED -- the real gap is far larger than "3x"** | External research (Barber/Odean, 15yr full-population Taiwan day-trader study): the elite top 0.1-0.5% of day traders sustainably earned 0.28-0.38%/day; the average retail day trader LOST -0.24%/day; ~89% of retail CFD accounts lose overall (AMF/ESMA disclosures). Our own validated backtest (~$7/day on $105.74 = ~6.6%/day) is already **17-24x above the best documented sustained real-world day-trading performance that exists in the literature.** $20/day (~19%/day) would be a further ~3x beyond that -- **~50-70x above anything credibly documented as achievable**, not a modest stretch goal. Two honest readings: (a) our backtest sits on an edge nobody has publicly documented (possible -- session-gated gold-specific work is genuinely narrower than generic day-trading), or (b) something in the backtest is still exaggerating results despite tonight's corrections. Does not invalidate tonight's work, but the target itself needs reframing -- as a $/day rate at $105 balance it is not defensible against any external evidence; as a target for a MUCH larger account (where the same edge, in %, produces $20/day) it may be reasonable. `2026-09-02, research/external_strategy_database.md Deep-dive pass 2` |
| **HYP-040** | The deployed portfolio's two NY legs (`fvg` and `range_rejection`) are effectively independent, so their combined survivability estimate holds. | **PARTIALLY REJECTED -- real correlation found, not modeled** | Checked directly: 34.7% of `range_rejection`'s NY signals (26 of 75) fire on the exact same bar as an `fvg` signal. `scripts/portfolio_merge_honest.py`'s chronological replay does not enforce a shared concurrent-position cap across legs, so it may have let both fire when live execution's real position cap would block the second. Net effect: the validated $703/~$7.03-day portfolio figure is likely a slight overestimate of what live execution actually produces, since some of the 26 overlapping opportunities would be blocked in practice -- a safety margin, not a danger, but the number should be read as an upper bound on this specific point. `2026-09-02, checked against src/research/candidates.py signal arrays directly` |
| **HYP-041** | The `fvg` (fair-value-gap) entry rule shares the look-ahead bias found in external FVG research (MPM Research: win rate collapses ~73%->50% when resolved on finer bars than the entry timeframe). | **REJECTED -- does not apply to our implementation** | Our `_c_fvg` (candidates.py:295-298) only detects whether a 3-bar gap EXISTS using strictly backward `_shift()` comparisons -- it fires immediately, with no "does price react as predicted" confirmation step to mis-resolve. The external bias is specifically about resolving a reaction outcome on coarse vs. fine bars; our exits already walk M1 sub-bars (Phase 0 finding), which is the fix the external research itself recommends. Checked directly against the source, not assumed safe. `2026-09-02` |

| **HYP-042** | portfolio_v4, deployed into main_loop.py, was ready to trade live as validated. | **REJECTED -- 3 of 4 legs would never have executed a single trade** | Traced real account history (the actual duplicate-order incident, 2026-08-31, that prompted this investigation) and found `main_loop.py` requires a "queue signal, confirm on the NEXT M15 candle" round-trip for every session except London -- `check_pending_confirmation()` must return the confirmed signal for `_execute_signal` to ever fire. Both `EMAStack` (before tonight) and all 4 `portfolio_v4` legs hardcode `check_pending_confirmation()` to always return `None`, meaning a queued signal is silently dropped forever. The backtest engine that validated every number in `PHASE3_FULL_SWEEP_RESULTS.md` has NO such mechanism at all -- it executes every signal immediately, unconditionally. Consequence: only the London leg (which used the pre-existing "execute immediately" path) would ever have traded; Asia/NY (3 of 4 legs, including the two best performers) would have been completely inert in production despite passing every earlier test. **Fixed**: added `execute_immediately` as an opt-in strategy attribute; `main_loop.py`'s session check now honors it (`is_london or getattr(strategy, "execute_immediately", False)`); all 4 `portfolio_v4` legs set it. This matches what was actually backtested rather than introducing an untested confirmation delay. Caught by tracing real MT5 deal history end-to-end, not by code review alone -- the earlier "verified end-to-end" claim (testing `evaluate()` in isolation) was true but incomplete; it never exercised the full `main_loop` dispatch path. `2026-09-02, src/core/main_loop.py + src/strategies/portfolio_v4.py` |
| **HYP-043** | "Duplicate main_loop.py processes" (venv python.exe + global Python311 python.exe, same command, same launch second) are two independent competing bot instances -- a live risk requiring a fix before restart. | **REJECTED -- parent/child, not a duplicate** | Directly tested: launched a trivial process via the venv interpreter and checked `ParentProcessId` in Windows process listings. The global-interpreter process's parent IS the venv-launcher process (confirmed via `Get-CimInstance Win32_Process`) -- this is standard Windows venv-launcher redirect behaviour (the venv `python.exe` is a small stub that spawns the real interpreter as a child and waits), not two independent OS-level bot instances racing on the same account. Retracts the "kill one, it's a duplicate" framing used earlier tonight. The REAL duplicate-order incident on the account (2026-08-31, two MorningMomentum positions opened 61 seconds apart, magic 2005) is a single-process bug -- exactly the documented "signal re-fires 57-61s apart" pattern in `src/backtesting/engine.py`'s own docstring, caused by `main_loop`'s per-candle dedup not covering every code path (see HYP-042's finding for the specific gap in the confirmation flow). `2026-09-02` |

| **HYP-044** | The manual chronological-replay portfolio test (HYP-036) overstated PF/net$ by not enforcing the engine's real shared concurrent-position/margin cap across the two overlapping NY legs (HYP-040's concern). | **REJECTED -- the opposite happened** | Wired genuine per-strategy TP/SL into `src/backtesting/engine.py` (`_open` now accepts `sl_atr_mult`/`tp_atr_mult`, backward-compatible, 4 new tests, 58/58 pass) and ran all 4 `portfolio_v4` legs through ONE real `engine.run([...])` call with the engine's actual shared caps (max_concurrent=3, max_same_direction=2) instead of each leg's artificially-tight isolated `max_concurrent=1`. Result: **PF 1.479 (vs 1.512), net $844 (vs $703, +20%), max drawdown 22.8% (vs 35.0%, 12pp shallower), min balance $105.74 (unchanged, never dropped below start)**. The shared cap let ASIA/LONDON legs take MORE trades than their isolated backtests allowed, which mildly outweighed the NY-overlap effect HYP-040 flagged. **This is now the strongest validated result of the entire research effort**, and it is the first result validated through the real engine loop end-to-end rather than a workaround. `2026-09-02, reports/PHASE3_ENGINE_MULTISTRATEGY_FIX.md` |

| **HYP-045** | portfolio_v4, restarted live after the execute_immediately fix (HYP-042), would begin evaluating and trading normally. | **REJECTED -- a second, separate silent-block bug found live** | `main_loop.py` fetches exactly 250 M15 bars (its documented live history window). `_SessionSpecialist._min_bars` was set to 260 -- copied from `EMAStack`'s own convention without checking against what `main_loop` actually supplies -- so `len(m15_rates) < self._min_bars` was true on every single call, and all 4 strategies returned `None` unconditionally, forever, with no error and no log line to indicate why. Found by direct step-through of the live startup sequence (`mt5.initialize()` -> `DataFetcher` -> each strategy's `evaluate()`), not by inspection. **Fixed**: `_min_bars` lowered to 250, verified all 4 strategies now correctly pass the bar-count check and evaluate normally. Caveat carried over from `EMAStack`'s own original docstring: EMA200 (used by the `EMASTACK_LONDON_TIGHT` leg) does not fully converge within 250 bars -- a known, accepted limitation of the live system's fixed history window generally, not new to this fix. The other 3 legs (squeeze_break, fvg, range_rejection) don't depend on EMA200 and are unaffected by the convergence caveat. `2026-09-02, src/strategies/portfolio_v4.py` |
| **HYP-046** | The 15/19-loss, 21.1%-WR result replaying portfolio_v4 over the two rest days (2026-09-03/04, the bot was stopped both days) reflects the strategy behaving as validated, not a bug. | **PARTIALLY REJECTED -- a real re-fire defect found in one leg, fixed; the rest is the validated shape** | Traced every one of the 19 simulated trades individually. `EMASTACK_LONDON_TIGHT` fired 5 near-identical BUYs between 11:45-13:30 IST on 09-03, all stopped at ~-1R: `XAU-005` (`_c_ema_stack`, candidates.py:132) is a STATE test (`ema20>ema50>ema200 and close>ema20`), true for as long as the ribbon stays aligned -- not an event marking when it starts. `main_loop.py`'s only dedup (`last_fired_candle[strategy]==signal_candle`, main_loop.py:316) blocks re-acting on the *same* candle, not "this is the same persisting state as my last trade" -- so every 15 min the ribbon stayed stacked through a sideways grind, `evaluate()` returned a "fresh" signal and opened another $6-stop position into the same failing thesis. **Fixed**: added an opt-in `edge_trigger` flag to `_SessionSpecialist` (`src/strategies/portfolio_v4.py`) requiring the previous bar's masked signal to differ before firing; enabled only on `EMAStackLondonTight`. First tried it as blanket plumbing on all 4 legs -- **made results worse** (net $17.98 -> -$22.19) because `fvg`'s and `squeeze_break`'s consecutive-bar repeats during 09-03 were genuine event chains inside real continuation moves, including the single biggest winner of the window (a chained FVG SELL sequence that ran to +5R) -- suppressing those cost more than the EMASTACK fix saved. Scoped to EMASTACK only: 17 trades (down from 19), WR 21.1%->**23.5%**, PF 1.186->**1.353**, net $17.98->**$29.85**, both legitimate winners untouched. **The remaining loss rate is real and expected**: `fvg`'s other 6 stopouts that evening were genuine per-bar gap events in a whipsaw NY session hitting a deliberately tight 0.5xATR stop (~$5) -- this portfolio's documented shape is "mostly small -1R losses, edge lives in rare big winners," consistent with its validated ~100-day PF **1.512**. Two days is too small a sample to grade the underlying edge either way. `2026-09-05, src/strategies/portfolio_v4.py` |
| **HYP-047** | `SYSTEM_FAULTS.md`'s "no real out-of-sample edge" verdict (4-year holdout PF 0.965, net -$96, `scripts/archive/rohith2_deadend/proximity_holdout.py`) still holds on today's code. | **REJECTED for today's code -- the original finding was real for the code that existed then, but HYP-046's fix changes the verdict** | Re-ran the exact same holdout script (`d1_ema20` gate, 2022-06-15 -> 2026-05-19) two ways to isolate the one variable that changed since 09-03. **Pre-fix** (`edge_trigger=False`, reproducing the original code): n=1383, PF **0.948**, net **-$99.02**, min balance **$6.72**, DD 96.1% -- a near-exact match to the original 0.965/-$96/$10 claim, confirming it was real. **Post-fix** (`edge_trigger=True`, today's code, HYP-046's EMASTACK dedup fix and nothing else changed): n=3715, PF **1.180**, net **+$1,720.51**, min balance $39.18, DD 83.9%. One bug fix -- not a re-tuned parameter -- flipped a losing 4-year holdout to a profitable one. Working theory for why trade count also rose (1383->3715) rather than fell: EMASTACK's pre-fix spurious re-entries were burning through the shared 6% daily-loss breaker more often, which blocks ALL FOUR legs for the rest of that day -- so the bug was also suppressing the other legs' legitimate opportunities, not just costing its own trades. Caveat: 83.9% max drawdown is still not survivable at real $100 stakes; this changes "does any edge exist" from no to yes, not "is it safe to run at $100." `2026-09-06, scratchpad holdout_variant.py / holdout_prefix_ab.py, same data+engine as proximity_holdout.py` |
| **HYP-048** | The D1 direction gate (`d1_ema20`) should stay disabled live, as set 2026-09-03 ("keep the demo bot trading"), now that HYP-047 shows a real edge exists to protect. | **Owner decision: gate stays OFF, made with the full cross-regime evidence on the table** | Tested gate ON vs OFF, today's post-fix code, live-matching caps (3 concurrent / 0.03 lot), two windows. **2025-01-01 -> 2026-09-04** (the recent regime): gate OFF n=3053, PF 1.245, net +$3,755, min bal $102.01 (barely dipped below the $105.74 start), $6.15/day; gate ON n=1743, PF **1.298**, WR **29.6%**, net +$2,482, min bal $75.52, maxDD **31.1%** (vs OFF's 40.3%), $4.06/day -- mixed, not a clean win either way in this one window. **2022-06-15 -> 2024-12-31** (older regime, gate OFF only tested here): PF **0.654**, net **-$101.24**, min balance **$4.50** -- the same gate-off config that made $6.15/day on 2025-2026 would have taken this account from $105 to $4.50 and never recovered on 2022-2024. Gate-ON's full-4-year figure (HYP-047, post-fix) survived that same older stretch at min balance $39.18. Also true and load-bearing: even inside the "recent, trusted" 2025-2026 window alone, `edge_by_period.py` (re-run fresh this session) shows Jan-May 2026 alone scored PF 0.914/net -$112/**101.3% drawdown** in isolation, immediately followed by May-Aug 2026 at PF 1.508/+$744 -- i.e. the window being trusted as "the current stable environment" already contains its own full boom-bust cycle. **Decision, made by the owner with all of the above shown explicitly**: keep the D1 gate OFF, on the explicit reasoning that the pre-2025 regime is considered no longer representative and the recent environment is the one to build around, even though the historical evidence available says gate-off has previously produced a near-total account wipeout in a different regime and gate-on has not. This is recorded as a conscious risk-acceptance decision, not a validated finding that gate-off is safe -- if the market reverts to something resembling 2022-2024 or Jan-May-2026-style conditions after real money is live, this is the specific, named risk being carried. `2026-09-06, owner decision` |

| **HYP-049** | Under the owner's 2-year standard (2026-09-06), portfolio_v4 post-fix clears Part I.1's out-of-sample holdout gate. | **SUPPORTED -- the first Part I gate this project has ever cleared** | Holdout defined honestly as **2025-01-01 -> 2026-05-20** (16.5 months): inside the regime the owner considers representative, but *excluding* the 2026-05-21..08-29 window the 4 legs were selected on, so the strategy is not scored on its own training data. Live config (D1 gate OFF, 3 concurrent, 0.01 fixed lot). Result: n=2460, WR 27.9%, **PF 1.2581**, net **+$3,029.80**, min balance **$102.01** (never dipped meaningfully below the $105.74 start), max drawdown **40.26%**. Against I.1's stated bar (PF > 1.2, maxDD < 45%, never below 50% of start): **PASS on all three.** *Alarming reference point, recorded because it cuts against the result*: the same config on the selection window itself (2026-05-21..08-29) scores **PF 0.6197, net -$104.21** -- the strategy now LOSES on the most recent 3 months while winning on the 16.5 months before it. That is the shape of edge decay, not of overfitting, and it is the single most concerning number in this session. `2026-09-06, scripts/validation/part1_suite.py holdout` |
| **HYP-050** | The edge is real, so the system is close to deployable. | **REJECTED -- the edge is real AND the account is a coin-flip on ruin; these are separate facts** | I.3 Monte Carlo (10,000 bootstrap resamples of the holdout trade sequence): PF distribution is genuinely healthy -- **p5 1.14, p50 1.258, p95 1.386** (even the bad tail is profitable). Risk is not: **median max drawdown 74.5%**, P(touch -50%) **54.2%**, **P(ruin) 39.4%** against a pass bar of <1%. I.2 walk-forward across 16 rolling 30-day windows: median PF **1.082** (bar >1.2, FAIL), 9/16 windows profitable, with two catastrophic months (-$1,172 and -$1,210). I.7 cost stress: PF **1.194** at 2x modeled cost -- fractionally under the 1.2 bar, but degrading gracefully (1.258 -> 1.229 -> 1.194 -> 1.160 at 1x/1.5x/2x/3x), so the edge is not merely a cost artifact. I.6 regime: per-leg on the holdout, **FVG_NY PF 1.466 (+$1,497)**, SQUEEZE_ASIA 1.242 (+$1,412), EMASTACK 1.102 (+$144), **RANGEREJECTION 0.982 (-$23, a net loser over 300 trades)**; by volatility quintile the strategy earns in high vol (q3 1.307, q4 1.264) and bleeds in mid vol (q2 0.979). `2026-09-06, scripts/validation/part1_suite.py montecarlo/walkforward/cost/regime` |
| **HYP-051** | Part II's sizing ladder (1 position at balances under $200, instead of 3) fixes the ruin problem HYP-050 found. | **REJECTED -- it makes it marginally worse** | Built the full Part II rulebook as code (`src/core/risk_rules.py`: ladder, daily/weekly/monthly/peak caps, consecutive-loss breakers, 20% margin cap; `ENFORCE=False` so it changes nothing live until validated). Tested the ladder's own sizing at $105.74 (0.01 lot, 1 position): holdout PF 1.237, net +$1,352, and **P(ruin) 42.4% -- versus 39.4% at the live 3-position config**. Reducing concurrency cuts trade count (2460 -> 1200) and profit but leaves risk-per-trade untouched, and risk-per-trade is what drives ruin. **The decisive follow-up** (`scripts/validation/survivable_size.py`): sweeping risk scale on the same edge, P(ruin) only clears 1% at **scale 0.15 (~$670 account)**, where the median return falls to **$0.90/day**. Survivability and daily return are the same dial. The one executable fix at $100 today is an **Exness Cent account** (XAUUSDc, 0.10 cent-lot = 1/10th notional): P(ruin) **0.03%**, ~$0.60/day. This was already Part II.1 option (B) and was never acted on. Also surfaced: the live config runs **62.4% margin utilisation** against the readiness doc's own **20%** cap. `2026-09-06, src/core/risk_rules.py + scripts/validation/{part1_suite ladder, survivable_size}.py` |
| **HYP-052** | The "$100 -> $130 in one day" sprint blueprint (0.75x ATR stop, 3.0x ATR target, halt at $80) is a viable way to hit the daily goal. | **REJECTED -- it hits its stop more often than its target** | Replayed all **355 real trading days** in the holdout, each starting fresh at $100, walking that day's actual trade sequence until it touched a barrier. Hit **+$30 target first: 102 days (28.7%)**. Hit **-$20 halt first: 131 days (36.9%)**. Neither: 122 (34.4%). **target:halt ratio 0.78** -- the premise requires it comfortably above 1. Median day **-$3.72** (mean +$2.99, carried by rare outliers up to +$146.81); only 44.2% of days finish green. The plan's arithmetic ("two $9 losses and one $36 win = +$18") is sound about a sequence it assumes but never establishes the frequency of. Full write-up incl. what the plan gets right: `docs/research/MONDAY_PLAN_ANALYSIS_2026-09-06.md`. `2026-09-06, scripts/validation/sprint_100_to_130.py` |
| **HYP-053** | D1_SYSTEM_PLAN.md's Phase 1 gate passes, so the macro/D1 replacement direction is viable. | **PARTIALLY -- 3 of 4 pre-registered hypotheses died; the 4th survives on a technicality that needs one more test** | Adjudicated the previously-blank verdict in `D1_MARKET_STUDY.txt` against the plan's own criteria. **H1 time-series momentum: REJECTED, sign is backwards** -- significant *reversion* at 21d (beta -0.0939, t **-7.56**), 63d (t -3.72), 126d (t -2.88); the only momentum-signed lookback (252d) is insignificant (t +1.36) and unstable by era (62.8% -> 47.4% sign agreement). **H3 real-yield: REJECTED** -- corr -0.163 and the conditional runs backwards (gold +21.8bp after yields fell vs +30.1bp after they rose). **H4 volatility breakout: NOT SUPPORTED** -- forward returns by vol regime are +45.7/+40.5/+45.1bp against 308-380bp standard deviations, i.e. noise. **H2 DXY: the survivor.** Section 5 only measured *contemporaneous* correlation (-0.397), which is not tradeable, so the missing lead/lag test was written and run (`scripts/d1_study/dxy_leadlag.py`, pre-registered pass criteria before running): prior-1d DXY -> forward-1d gold gives beta **-0.1099, t -3.82**, correct negative sign, sign stable across all five eras -> **passes as written**. Honest caveats recorded: per-era t-stats are mostly individually insignificant (-1.38/-2.99/-1.47/-1.37/**-0.55** in 2022-2026, i.e. weakest in the most recent era), and the cell was the strongest of 20 tested (5 lookbacks x 4 horizons), so it carries a real multiple-testing discount. Also noted: the D1 study's random-entry control sets the bar at **95th-pct PF ~1.27-1.29**, which portfolio_v4's own holdout PF (1.258) would *not* clear. `2026-09-06, docs/research/D1_MARKET_STUDY.txt + scripts/d1_study/dxy_leadlag.py` |

| **HYP-054** | Restricting to 11:30-21:30 IST (owner decision 2026-09-06, "no night trades") simply removes the Asia leg and leaves the rest intact. | **PARTIALLY REJECTED -- it halves profit AND makes drawdown worse; but it points at a much better configuration** | Measured on the HYP-049 holdout, live config, four leg combinations. **A) all 4 (current)**: n=2460, PF 1.258, net $3,030, maxDD 40.3%, $6.00/day, P(ruin) 39.4% -- I.1 **PASS**. **B) 11:30-21:30, Asia dropped**: n=1828, PF 1.256, net $1,601, maxDD **66.2%**, $3.17/day, P(ruin) 28.1% -- I.1 **FAIL**. Removing SQUEEZE_ASIA halves net profit *and* worsens max drawdown 40% -> 66%: the Asia leg was providing genuine diversification that smoothed the equity curve, not just adding trades. **C) Asia and RangeRejection both dropped (EMASTACK+FVG)**: n=435, PF **0.907**, net **-$91**, min balance **$14.52**, P(ruin) 73% -- a disaster, and a counter-intuitive one worth naming: FVG *alone* makes +$1,504, EMASTACK contributes +$144 inside the full portfolio, yet the two together LOSE. The mechanism is the shared 6% daily-loss breaker -- EMASTACK's losses burn the day's budget and block FVG's better trades later, the same cannibalisation found in HYP-047. **D) FVG_NY alone**: n=1156, **PF 1.432 (best of any variant)**, net $1,504, maxDD 49.6%, $2.98/day, **P(ruin) 11.6% (lowest of any variant, 3.4x better than the current portfolio)**, sits entirely inside 17:30-21:30 so it is fully compliant with the new window. It still fails I.1 on drawdown (49.6% vs the 45% bar). **Read: under the owner's session rule, the best available configuration is FVG_NY alone, not the current portfolio minus Asia.** Combined with the HYP-051 finding (Cent account at 1/10th risk), FVG-only on a Cent account is the first combination in this project's history that would be both compliant and genuinely survivable. Not yet validated as such -- that is the next test. `2026-09-06, scripts/validation/session_1130_2130.py` |

| **HYP-055** | No configuration of this system can pass both the edge gate (I.1) and the survivability gate (I.3) at $100. | **REJECTED -- one configuration passes both. First time in this project's history.** | Assembled from measurement, not preference: **FVG_NY_TIGHT alone** (HYP-054: best PF and lowest ruin of every leg combination, and its 17:30-21:30 session sits entirely inside the owner's 11:30-21:30 rule), **one position at a time** (also what Part II's ladder mandates under $200), on **Cent-account sizing** (HYP-051: risk-per-trade drives ruin, and 0.01 lot is the Standard-account floor). Holdout 2025-01-01 -> 2026-05-20: n=990, WR 23.5%, **PF 1.5004**, net +$1,477, **min balance $104.20 -- never once dipped below the $105.74 start**, maxDD **35.69%**. **I.1: PASS** on all three conditions. Monte Carlo by risk scale: Standard 0.01 lot P(ruin) 8.59% (I.3 FAIL); **Cent 0.25 lot P(ruin) 0.000%, 95th-pct DD 32.4%, $0.73/day -- I.3 PASS**; Cent 0.10 lot P(ruin) 0.000%, 95th-pct DD 15.3%, $0.29/day -- I.3 PASS. Two further points of note: PF 1.50 also clears the D1 study's random-entry bar (95th-pct PF 1.27-1.29), which the full 4-leg portfolio's 1.258 did **not**; and I.4 parameter perturbation on the full portfolio gives worst-case PF 1.129 across the +/-20% cloud (**FAIL** vs the 1.2 bar), but asymmetrically -- *tightening* stops improves PF (0.8x -> 1.284, 0.9x -> 1.269) while widening degrades it (1.1x -> 1.194, 1.2x -> 1.129), so the parameters are not knife-edge, they are simply better slightly tighter. **Honest framing: this is a survivable configuration earning roughly $0.29-0.73/day on $100, not a path to $20-30/day.** It is the first thing here that could be run with real money without a coin-flip on ruin. Not yet paper-tested; I.9 (8 weeks frozen) remains the outstanding gate. `2026-09-06, scripts/validation/candidate_config.py` |

| **HYP-056** | The Monday Battle Plan's proposed entry rule (EMA20/50/200 stack + 60% rejection wick + pullback, SL 0.75x / TP 3.0x ATR, 11:30-21:30) is worth implementing for Monday's open. | **REJECTED -- implemented exactly as specified and it loses money** | The plan's sprint *mechanics* were already rejected in HYP-052 (hits its -$20 halt on 36.9% of real days vs its +$30 target on 28.7%; median day -$3.72). This tests the *entry rule itself*, which had never existed in code. Built to the plan's own spec and run on the HYP-049 holdout (1 position, 0.01 lot). The plan contradicts itself on the pullback reference -- prose says EMA20, pseudocode tests `low <= ema50` -- so both were run. **As pseudocoded (EMA50): n=377, WR 20.4%, PF 0.9271, net -$89.52, min balance $16.22, maxDD 93.8%, P(ruin) 76.2%.** As prose reads (EMA20): n=702, WR 21.8%, PF 1.0664, net +$171.57, min balance $46.70, maxDD 87.4%, P(ruin) 62.7%. Both fail I.1. For contrast, the leg the system already owns -- FVG_NY alone (HYP-055) -- returns PF 1.5004, +$1,477, min balance $104.20, maxDD 35.7%, P(ruin) 8.6%, and PASSES I.1. The proposed rule's measured win rate (20-22%) also falls short of the 35-45% the plan's own section 2 cites as the legitimate benchmark. **What the plan gets right is recorded too** (it is not a bad document): its debunking of 80-90% win-rate claims matches HYP-039; its "a fixed $5 stop is guaranteed to be hit by noise" is independently confirmed by our NY noise floor ($8.69) and the 26-year D1 study; and its Part 6 conclusion that "the gap is capital, not strategy" is exactly what HYP-051's risk-scale sweep measured. Full analysis: `docs/research/MONDAY_PLAN_ANALYSIS_2026-09-06.md`. `2026-09-06, scripts/validation/monday_sprint_strategy.py` |

| **HYP-057** | A dynamic stop (structure-based, noise-floor-aware, gap-proportional, or volatility-regime-adaptive) beats FVG_NY's static 0.5x ATR stop, since that stop sits below NY's own measured noise floor. | **REJECTED -- static 0.5x ATR remains best of 12 schemes tested, through the real engine** | First pass used a hand-rolled M15-bar resolver and gave a baseline of PF 0.56 for the CURRENT live config -- which contradicted the already-established engine result of PF 1.5004 for the identical strategy and window. Diagnosed and discarded: the resolver assumed SL-before-TP whenever an M15 bar's range straddled both levels, which converts real winners into losses when the stop is only ~$4-5 against $9+ M15 ranges -- exactly the M1-fidelity problem the engine's own sub-bar walk (COR-001) exists to prevent. Rebuilt as real strategy subclasses with a per-signal dynamic `sl_atr_mult`, resolved through `BacktestEngine` (M1 fidelity). Result on the 2025-01-01 -> 2026-05-20 holdout: static 0.5xATR (current) **PF 1.5004, P(ruin) 8.59% -- best of 12**; every wider/adaptive scheme scored worse, several catastrophically (structure-based: PF 0.80-0.90, P(ruin) 71-75%; static 1.0-1.25xATR: PF 0.84-0.87, P(ruin) ~72%). Mechanism identified: a wider stop doesn't just cost more per loss, it draws the account down faster and trips the shared 6% daily breaker more often, which blocks the leg's OWN later good trades -- trade count collapsed from 990 (0.5x) to as low as 130-169 (1.0-1.25x, structure) as a direct result. Only gap-proportional 1.0x (PF 1.3694, P(ruin) 17.36%) came close and still lost on every metric. `2026-09-07, scripts/validation/fvg_dynamic_stops_engine.py` |
| **HYP-058** | The "[SMT] Renko Buy & Sell" strategy (reported PF ~1.9 on TradingView) has a real edge, or at minimum its TradingView result is explained by the well-documented Renko lookahead artifact (backtester fills at the synthetic brick price, a level the market already left in real time). | **REJECTED -- fails in every honest configuration tested, AND the lookahead-fill reproduction did not explain the TV number either** | Built ATR(10)-brick Renko from real M15 bars two ways: bricks resolved at the real bar close where the flip first becomes knowable ("honest"), and at the synthetic brick boundary price ("lookahead," mimicking what `request.security(ticker.renko(...))` returns). First attempt with brick size re-evaluated every bar caused a MemoryError (ATR occasionally reads $0.6-1.3 on this data, letting one wide-range bar spawn unbounded bricks) -- fixed by using a single fixed brick size (median ATR(10) over the test window), the more standard reading of "ATR-based Renko" in any case. Result, 2025-01-01 -> 2026-05-20: honest reversal PF **0.066**; honest + ATR stops 1.0x/3.0x PF **0.809**; lookahead-fill PF **0.015** -- worse than honest, not better, so the lookahead artifact cannot be credited with the reported TradingView number on this data. Caveat carried honestly: gold roughly doubled in price across this window, and a brick size fixed at the window's median degrades badly as price outgrows it -- a real modeling difficulty for Renko specifically, not dismissed as a non-issue. Whatever produced PF 1.9 on TradingView, it isn't reproducible against this project's own price data in any tested configuration. `2026-09-07, scripts/validation/renko_test.py` |

| **HYP-059** | LuxAlgo-style liquidity context (pasted 'Buyside & Sellside Liquidity' Pine script) improves FVG_NY as a FILTER, not a trigger -- the owner's own correct framing ("determine the market better," not an execution strategy). | **SUPPORTED, and not NY-specific** | Ported two ideas as filters on the unchanged FVG signal: SWEEP (gap forms within 12 bars of a clustered liquidity level, 3+ pivots within atr/margin, being breached -- a footprint of real forced flow) and VOID (the script's own literal definition: gap size > ATR200, a size floor set by the market's own long-run volatility). Tested across every session, full engine (M1 fidelity), 2025-01-01 -> 2026-05-20: NY sweep-or-void PF 1.593 (vs 1.500 unfiltered), maxDD 31.2% (vs 35.7%), P(ruin) 5.6% (vs 8.6%) -- better on every axis. **London, unfiltered, fails I.1 outright** (PF 1.214, maxDD 79.8%) -- **with the sweep filter it passes** (PF 1.311, maxDD 33.8%, P(ruin) 19.5%). Asia improves too (void filter: PF 1.352 vs 1.299, maxDD 39.2% vs 41.8%). The filter doesn't just help the session it was found on; it rescues a session (London) that was otherwise unusable. `2026-09-07, scripts/validation/liquidity_filter.py + fvg_all_sessions.py, src/research/liquidity.py` |
| **HYP-060** | The owner's requested set of "strategies for next week" (NY unfiltered + NY sweep + NY void + NY sweep-or-void, run as separate concurrent legs) is a valid 4-leg portfolio. | **REJECTED before being built -- the four are nested subsets of one signal, not independent strategies** | `sweep` and `void` are each strict subsets of the unfiltered FVG rule (they only add a restriction, no candidate signal fires under a filter that wouldn't also fire unfiltered); `sweep_or_void` is a superset of both. Running all four concurrently would fire 2-4 positions on the SAME underlying gap, multiplying exposure on one event -- not diversification, and not what any of the isolated backtest numbers measured. Corrected to one leg per session, chosen for the best measured risk profile: `sweep_or_void` for NY (dominates plain NY on drawdown and ruin while keeping most of the profit), `sweep` for Asia (the better of Asia's two filter options). `2026-09-07, src/strategies/portfolio_v4.py` |
| **HYP-061** | The corrected 2-leg portfolio (FVGAsiaSweep + FVGNYSweepOrVoid) survives being combined on one real account, not just in isolation. | **SUPPORTED -- combining them did not cannibalize, unlike EMASTACK+FVG (HYP-054)** | Implemented as production strategy classes (`_LiquidityFilteredFVG` in `src/strategies/portfolio_v4.py`, liquidity logic in the new `src/research/liquidity.py`), replacing the single-leg `FVGNYTight` as `PORTFOLIO_V4`. Combined backtest, live config (3 concurrent, D1 gate off, 11:30-21:30 window), same holdout: **n=1424, PF 1.3785, net +$1,500.59 (vs $1,439.48 naive sum of the isolated legs -- slightly BETTER combined, the shared caps let both legs take a few more trades than their isolated tests allowed, same positive mechanism as HYP-044), min balance $100.60 (barely dipped below the $105.74 start), max drawdown 33.45%, I.1 PASS.** Monte Carlo: P(ruin) **12.38%** -- higher than NY-alone's isolated 5.6% (a second leg adds exposure), but a **3.2x improvement over the old 4-leg portfolio's 39.4%** (HYP-050) and the best combined-portfolio risk profile this project has produced. Registered in `validation_ledger.json` -- `main_loop.py`'s config-integrity gate (IV.11) now recognizes this exact configuration as validated and will start; it correctly refused to start on the un-recorded new strategy set beforehand, confirmed by direct check. `2026-09-07, scripts/validation/combined_portfolio_v5.py` |

| **HYP-062** | Running all 5 owner-requested FVG/liquidity variants as separate concurrent legs (rather than the deduplicated 2-leg version, HYP-060/061) would multiply exposure on overlapping gaps and hurt risk metrics. | **REJECTED -- the mechanism predicted was real, but the consequence was the opposite of expected** | HYP-060 substituted a deduplicated 2-leg portfolio for the owner's explicit 5-leg request, reasoning that `sweep`/`void`/`sweep_or_void`/unfiltered FVG_NY are nested subsets of one signal and would fire redundantly. Corrected on direct instruction: built all 5 legs (`FVGAsiaSweep`, `FVGNYTight`, `FVGNYSweep`, `FVGNYVoid`, `FVGNYSweepOrVoid`) and measured the actual combined effect through the real engine rather than assuming it. Confirmed the overlap is real: 604 candles saw 2+ NY legs fire on the identical underlying gap, 1,793 trades stacked on them. **But the combined result beats the deduplicated 2-leg version on every axis but one**: PF 1.4847 (vs 1.3785), net **+$4,100.40** (vs $1,500.59), max drawdown **29.95%** (vs 33.45%), **P(ruin) 8.63%** (vs 12.38%), **$8.12/day** (vs $2.97). The one cost: min balance **$97.52**, a shallow ~$8 dip below the $105.74 start that the 2-leg version didn't have. Mechanism: stacking only happens when a gap is good enough to also pass the stricter filters (sweep/void) alongside the unfiltered baseline -- so overlap functions as automatic quality-weighted sizing (bet bigger, up to the 3-position cap, on higher-conviction setups) rather than redundant risk on random ones. All 5 legs individually net-positive (PF 1.25-1.66 each). Registered as the live configuration. Caveat unchanged from every other result this session: one 504-day window; the stacking-as-sizing behavior is a measured mechanical effect here, not a guaranteed property of the approach in a different regime. `2026-09-07, scripts/validation/combined_portfolio_5legs.py, src/strategies/portfolio_v4.py` |

| **HYP-063** | `FVG_ASIA_SWEEP` can stay in `PORTFOLIO_V4` alongside the 11:30-21:30 "no night trades" trading window (HYP from 2026-09-06). | **REJECTED -- the two don't overlap at all; benched, not deleted** | Asia's session is (2.5, 11.5) IST; the enforced trading window is (11.5, 21.5) IST -- they share only a single boundary point, so `market_hours.py` would block every Asia signal, every time, live. Same silent-failure shape as HYP-042/045 (signal fires, main_loop drops it, no error, nothing to notice). Caught before deployment by asking what a specific day (Friday 2026-09-04) would have done live vs as validated. **Fix**: `FVGAsiaSweep` kept in the codebase exactly as validated (PF 1.279, n=548, P(ruin) 17.4% in isolation) with a clear docstring on why it's benched and how to reactivate (widen the window, or run it on a separate unblocked schedule) -- owner explicitly asked not to lose the research. Removed from `PORTFOLIO_V4`; the remaining 4-leg NY-only config re-validated (below). `2026-09-07, src/strategies/portfolio_v4.py` |
| **HYP-064** | Removing FVG_ASIA_SWEEP (HYP-063) leaves the 4-leg NY-only portfolio at least as good as the 5-leg version. | **PARTIALLY REJECTED -- ruin probability improved, but the portfolio now fails its own I.1 gate** | Re-validated through the real engine, same holdout: n=2179, PF **1.5426** (up from 1.4847), net $3,492.19 (down from $4,100.40), min balance **$101.11** (better than $97.52), **P(ruin) 6.93%** (better than 8.63%) -- but **max drawdown 54.72%, failing I.1's own <45% bar** (5-leg version passed at 29.95%). Mechanism: all 4 remaining legs fire only in the NY session and correlate with each other; without Asia's different-session trades to fill in during NY's rough stretches, peak-to-trough drawdown deepens even though the probability of actual ruin is slightly lower. **Drawdown and ruin are not the same risk and can move in opposite directions** -- a finding worth keeping generally, not just here. Registered as the live config anyway, since it is what the window rule actually permits, with the I.1 failure recorded explicitly in the ledger entry rather than omitted. `2026-09-07, scripts/validation/combined_portfolio_5legs.py` |
| **HYP-065** | Trading FVG strategies without the D1 EMA20 trend filter (HYP-048) in a high-volatility environment like August 2026 causes terminal drawdowns, because the massive 5R targets cannot be reached against strong counter-trend momentum. | **SUPPORTED -- D1 Gate must be ON for FVG strategies** | Investigating a -79% drawdown in August 2026 revealed that the FVG strategies were taking valid counter-trend setups that spiked into profit (up to 4R) but reversed fully to hit their 0.5 ATR stops without trade management (trailing/BE). Breakeven and trailing stops failed to solve this (-$86 and -$84 net respectively) because the gaps simply did not have trend momentum. Re-enabling the D1 Gate perfectly filtered the toxic counter-trend setups, turning the -$83.87 loss into a +$44.00 profit, PF 1.40, and reducing max drawdown from 79.3% to 34.9%. Owner decision HYP-048 is explicitly reversed: the D1 gate is mandatory to prevent terminal drawdowns in volatile trending months. `2026-09-06, scripts/validation/last_month.py` |

| **HYP-066** | Pre-registered, single-hypothesis test (`docs/research/HYP-NY-FVG.md`): the NY-session FVG displacement carries a tradeable edge on its own, when tested honestly on a clean holdout it has never seen -- isolating the one component of the dead portfolio_v4 that ever showed OOS promise (`FVG_NY_TIGHT`, 0.5/2.5 ATR, IST 17:30-21:30). Does NOT touch the live system: own strategy class, own config, own script (`scripts/validation/hyp_ny_fvg.py`). | **REJECTED -- 0 of 6 gates passed, all three core gates failed. Hypothesis retired, no revision.** | Data split fixed before running: in-sample 2015-2020 (inspected: PF 0.487, account blown), holdout 2021-01-01 -> 2023-12-31 (the verdict, run once). Holdout headline: n=663, WR 13.9%, **PF 0.833, net -$92.97 on $100**, account effectively blown by mid-2022 (min bal $7.03), maxDD 95.6%. Gates: **(1) beats random entry -- FAIL**, real PF 0.833 vs random-entry p95 0.70, absolute floor 1.30; **(3) survivable -- FAIL**, MC P(ruin) 70.4%; **(5) regime-robust -- FAIL**, 2021 -$75 / 2022 -$18 / 2023 dead; (2) risk-adjusted FAIL (Sharpe -0.76); (4) not knife-edge FAIL (whole +/-20% cloud PF 0.73-0.83); (6) 2x cost FAIL (PF 0.59). Nuance recorded: real PF 0.833 does beat the random-entry distribution (p50 0.566), so a fresh 3-bar NY gap carries *a little* directional information -- but nowhere near enough to pay for a 0.5xATR stop on a 24h instrument, and random entry in the same session+geometry also loses (p50 PF 0.57). The 0.5/2.5 geometry that made FVG_NY_TIGHT look good on its 2026 selection window does not survive a clean 2021-2023 holdout. Per the pre-registration's own kill criteria, the intraday-FVG line is closed; next action is the D1 DXY lead/lag regression (`D1_SYSTEM_PLAN.md`, HYP-053 H2). `2026-09-08, scripts/validation/hyp_ny_fvg.py holdout` |

| **HYP-067** | Owner question (2026-09-09): "instead of the daily trend gate, use an intraday trend." Tests whether an H4- or M15-structure direction gate beats the live D1 EMA20 gate. | **REJECTED -- d1_ema20 is the best of four variants; intraday-structure gates do not beat it and M15-structure blows the account on recent M1 data** | Through the engine (`part1_suite.live_config`, validated 2-leg set TIGHT+SOV, 0.01 lot / 3 concurrent / 6% breaker / realistic costs / Highlander / Friday flatten). Broker terminal depth limits the test: YEAR window (2025-09-09 -> 2026-09-09) runs M5-coarse intrabar fills, RECENT (2026-06-09 -> 2026-09-09) has true M1; both overlap the 05-21..08-29 selection window, so this is a robustness question, not edge discovery. **YEAR**: none PF 1.33 / +$1047 / DD 28.9%; **d1_ema20 PF 1.58 / +$875 / DD 22.7% / min $93**; h4_structure PF 1.50 / +$724; m15_structure PF 1.44 / +$868 / min $85. **RECENT (true M1)**: none **PF 0.83 / -$84 / blown to $21**; m15_structure PF 0.87 / -$59 / blown to $47; h4_structure PF 1.11 / +$38 / min **$85**; **d1_ema20 PF 1.10 / +$41 / min $102 (never below the $105.74 start)**. d1_ema20 is the only variant solvent with a shallow drawdown on recent M1; "no gate" blows the account -- independent re-confirmation of HYP-065. Keep the D1 EMA20 gate. `2026-09-09, scripts/validation/intraday_trend_gate_ab.py` |
| **HYP-068** | Owner claim (2026-09-09, from another tool): snapping the take-profit to local BigBeluga swing pivots instead of the fixed ATR multiple turns FVG_NY_TIGHT into PF ~4.3 / $100 -> $12k in a year. | **REJECTED -- the PF 4.3 is vectorised-backtest lookahead; through the real engine a lookahead-free structural TP marginally HURTS every leg combination** | Built `structural_tp` into `BacktestEngine` (engine.py): at entry the TP is pulled IN to just before the nearest CONFIRMED swing pivot in the trade direction (`structure.compute` on the backward-looking m15_view, confirmation already lag-shifted -- no lookahead), used only when its distance is in [1.0xATR, static ATR TP) so it can only bring the target closer, never reach further; SL untouched. 5 leg combos x 2 windows, live config. **Every cell: struct <= static.** tight/year 1.5835 -> 1.5809 (net $875 -> $854, 87 TPs snapped); sov/year 1.6362 -> 1.5909 ($550 -> $506); sov/recent 1.31 / +$72 -> 1.22 / +$50; 3leg/year 1.20 / $713 -> 1.12 / $424 (167 snapped). Mechanism: these strategies win 18-27% of trades and live on the rare 2.5R runners; snapping the target closer converts would-be big winners into ~1.5R and PF falls. Same shape as HYP-057 (dynamic stops) and HYP-058 (Renko) -- a "smarter exit" that dies in the real engine. Static ATR TP stays. `2026-09-09, scripts/validation/structural_tp_ab.py` |
| **HYP-069** | `EMAStackLondonTight`, re-added to `PORTFOLIO_V4` on 2026-09-09 as a non-NY-session diversifier (owner decision), is a net benefit to the portfolio. | **REJECTED on the data -- EMASTACK is net-negative solo AND poisons FVG_NY_TIGHT through the shared 6% daily breaker; the 3-leg portfolio loses money on both test windows where the 2-leg does not. Live-config change pending owner decision.** | `_recent_diag.py` + `structural_tp_ab.py`, engine, live config. **RECENT (2026-06-09 -> 2026-09-09, true M1)**: 3-leg LIVE n=113, PF 0.88, **net -$85.21, blown to $20.53, DD 93%** (per leg: EMASTACK n=41 PF 0.75 -$110.61 | SOV n=33 PF 1.56 +$75.39 | **TIGHT n=39 PF 0.68 -$49.98**). 2-leg (TIGHT+SOV) n=106, PF 1.10, **+$40.64, min $102.20 (never below start), DD 50%** (SOV +$21.81 | TIGHT +$18.83). TIGHT alone PF 1.10 +$40.64. **SOV alone PF 1.31, +$72.08, min $130.63, DD 39% -- best of every config tested.** EMASTACK alone PF 0.75, -$95.80, blown to $9.94. **YEAR (M5-coarse)** agrees: 3-leg PF 1.20 / +$713 / DD 56% vs 2-leg PF 1.58 / +$875 / DD 23%; EMASTACK solo PF 0.81, min -$47. Mechanism (same cannibalisation as HYP-047 / HYP-054): EMASTACK's London losses burn the day's 6% loss budget, so when NY opens the breaker is tripped or near it and FVG_NY_TIGHT's later, better trades are blocked or degraded -- TIGHT flips from +$41 solo to -$50 in the 3-leg. SOV's liquidity filter keeps it selective enough to still land its few good trades, which is why it barely moves. "Session coverage" is real but it is buying a losing London book at the direct cost of the winning NY book. Recommendation: revert `PORTFOLIO_V4` to 2-leg (TIGHT+SOV), or SOV-alone. Caveat: RECENT is 3 months and overlaps the selection window (only true-M1 data the broker terminal holds); YEAR is M5-coarse -- both point the same way. **Owner decision 2026-09-11: keep the 3-leg config live as-is.** The EMASTACK drag on risk-adjusted return and the shared-breaker cannibalisation of FVG_NY_TIGHT are accepted as a known cost, in exchange for London-session coverage the NY-only legs cannot provide -- a conscious risk-acceptance decision, recorded as such (same category as HYP-048), not a finding that the 3-leg config is safe. If the account draws down through the week of 2026-09-11, this is the named cause. `2026-09-11, scripts/validation/_recent_diag.py` |
| **HYP-070** | Owner request (2026-09-11): "liquidity sweep, and market trend reversal ... try creating mathematical variations." A parameterised failed-breakout / stop-run fade -- price spikes through a swing extreme (rolling high/low, confirmed pivot, or pivot cluster), closes back inside, reversal trades the return. | **REJECTED -- no structural edge under any of 22 mathematical variations x 2 windows, and no variant is positive in more than 2 of 5 regime eras over 22 years. Joins the graveyard with Range_rejection_wick / Failed_breakout_reversal.** | Built `src/strategies/liquidity_sweep_reversal.py` (level type x lookback x penetration x close-reject x confirmation {immediate/next-bar/displacement/CHoCH} x regime {none/ADX<22/counter-EMA200} x session x SL/TP geometry x D1-gate). All through `BacktestEngine`, $100 / 0.01 lot. **4-year matrix (2022-2026 broker M15, `liq_sweep_reversal_matrix.py`)**: base PF 0.91; 15 of 22 cells draw the account below $15. Two cells looked alive -- `confirm=displacement` PF 1.10 / +$161 / min $92, and combo `displace + counter-EMA200 + SL0.75/TP2.5` PF 1.48 / +$230 / min $102 -- but the recent-M1 window disagreed with the 4-year window on nearly every cell (regime dependence, not edge). **22-year regime test (Kaggle set, 5 eras each fresh $100, `liq_sweep_22yr.py`)**: base 0/5 eras positive medPF 0.79; **`displace` 0/5 medPF 0.73** (the +$161 was a 2022-2026 artifact); `displace+range` 2/5 (thin, medPF 0.71); `choch` variants n<12 per era, unusable; **the PF 1.48 combo -> 1/5 eras positive, medPF 0.71** (positive only in the era it was selected on -- textbook selection bias, same as portfolio_v4 / HYP-066). Conclusion: on gold M15 the fade of a swept level has negative per-trade expectancy after costs in every regime, under every variation tried. Full write-up `docs/research/LIQ_SWEEP_REVERSAL_RESULTS.md`. `2026-09-12, scripts/validation/liq_sweep_reversal_matrix.py + liq_sweep_22yr.py` |
| **HYP-071** | (autonomous overnight, adjudicated 2026-09-12) The live 0.5xATR FVG stop is too tight for 2026 volatility (~$5 stop vs ~$10 ATR -> 13-18% win rate); a wider stop would improve the FVG legs. | **REJECTED -- 0.5xATR is the best stop of every value tested; every widening degrades PF and inflates drawdown on both windows.** | `fvg_geometry_sweep.py`, engine, live config, SOV + TIGHT (real classes, per-instance mult mutation), YEAR (2025-09-09..2026-09-09, M5-coarse) + RECENT (2026-06-09..2026-09-09, M1). YEAR **SOV**: SL0.5/TP2.5 PF 1.64 / DD 19% -> SL0.75 PF 1.25 / DD 30% -> SL1.0 PF 1.16 / DD 49% (fails I.1) -> SL1.25 PF 1.06 / DD 66%. YEAR **TIGHT**: SL0.5 PF 1.58 -> 0.75 PF 1.31 -> 1.0 PF 1.16 -> 1.25 PF 1.08. RECENT tells the same story (SL0.75+ on TIGHT blows the account, min $11-18). Mechanism: a wider stop costs more per loss AND draws down faster, tripping the shared 6% daily breaker more often -- trade count and later good trades both collapse. The low win rate is the price of the ~5:1 win:loss asymmetry, not a fixable flaw. **One marginal positive**, not acted on: at SL0.5, nudging TP 2.5 -> 3.0/3.5 is slightly better on both windows (RECENT SOV PF 1.31 -> 1.45/1.49, i1_pass) -- a TP-only change worth an owner look, but it needs full I.1 + a new fingerprint. Keep SL 0.5xATR. `2026-09-11, scripts/validation/fvg_geometry_sweep.py` |
| **HYP-072** | (autonomous overnight, adjudicated 2026-09-12) The archived LuxAlgo finding -- a >=0.10% minimum-gap-size filter takes FVG_NY from PF 1.66 -> 2.12 -- reproduces on clean broker data. | **PARTIALLY -- the PF 2.12 does NOT reproduce (it came from the selection window + near-zero-spread data); but a smaller 0.05% gap floor is a real improvement on the recent window and neutral on the year.** | `fvg_gapsize_sweep.py`, engine, live config, baseline (unfiltered == TIGHT rule) + SOV, YEAR + RECENT. **baseline YEAR**: no filter PF 1.58 -> gap 0.05% PF 1.63 -> 0.10% PF 1.34 -> 0.20% PF 1.54 (n 412 -> 263 -> 148 -> 58). **baseline RECENT**: no filter PF 1.10 (fails I.1, DD 50%) -> **gap 0.05% PF 1.42, i1_pass, DD 30%** -> 0.10% PF 1.45 (n 45). **SOV RECENT**: 1.31 -> 0.05% 1.44 -> 0.10% 1.48. Nowhere near PF 2.1. But a **0.05% gap floor** lifts PF ~0.1-0.3 on recent, cuts ~35% of trades (less overtrading), and does not hurt the year -- the most promising FVG refinement on the table. Config change -> full I.1 re-validation + new fingerprint required before it goes live. `2026-09-11, scripts/validation/fvg_gapsize_sweep.py` |
| **HYP-073** | Owner instruction (2026-09-12): "re validate." Do the HYP-071/072 candidates (SOV TP->3.5, 0.05% gap floor) actually clear the real go-live gate (I.1 holdout + I.3 Monte Carlo + I.7 cost stress), for the live 3-leg shape and, as a reference, the 2-leg shape? | **PARTIALLY -- no config clears all three gates, but `combined_2leg` (TIGHT+SOV, 0.05% gap floor, SOV TP 3.5) is the FIRST config in this project's history to pass I.1 cleanly; it fails only I.3 on sequence risk. Every 3-leg variant -- with or without the HYP-071/072 improvements -- fails all three gates, independently re-confirming HYP-069.** | `revalidate_candidates.py`, real engine, live config (`max_concurrent=3`, live bot caps at 2 -- known fidelity gap, HYP-067/069), I.1 holdout 2025-01-01->2026-05-20 (pre-dates broker M1 depth, M5/M15-coarse fills), I.3 = 10k-path bootstrap of the holdout trade sequence, I.7 = 2x spread+slippage. Pass bars: I.1 PF>1.2 & maxDD<45% & minBal>50%; I.3 maxDD95<40% & P(ruin)<1%; I.7 PF>1.2.<br><br>| config | I.1 | I.3 | I.7 | PF / net / maxDD |<br>|---|---|---|---|---|<br>| baseline_3leg (LIVE) | fail DD61% | fail P(ruin)39% | fail | 1.22 / +788 / 61% |<br>| sov_tp35_3leg | fail DD71% | fail P(ruin)42% | fail | 1.21 / +748 / 71% |<br>| gap005_3leg | fail DD53% | fail P(ruin)44% | fail | 1.20 / +593 / 53% |<br>| combined_3leg | fail DD61% | fail P(ruin)45% | fail | 1.20 / +608 / 61% |<br>| baseline_2leg | fail DD51% | fail P(ruin)4% | **PASS** | 1.61 / +928 / 51% |<br>| **combined_2leg** | **PASS DD31%** | fail P(ruin)6.2% | **PASS** | **1.64 / +660 / 31%** |<br><br>Every 3-leg config -- baseline AND both improved variants -- fails all three gates, and fails them *worse* than the matching 2-leg config in every case (e.g. combined_3leg DD 61% vs combined_2leg DD 31%, same leg changes, only EMASTACK differs). This is the third independent line of evidence against EMASTACK (after HYP-069's per-leg attribution), now at the level of the actual go-live gate, not just P&L. `combined_2leg` is real progress -- first-ever clean I.1 pass -- but I.3 fails: the 570->329 trade reduction from the gap filter concentrates outcomes onto fewer, fatter-tailed wins, so the specific historical sequence looks smooth (DD 31%) while a bootstrap-reshuffled sequence has a 6.2% chance of still touching -80% (needs <1%). The holdout passing does not mean the edge is safe to size against; it means this specific ordering of 329 trades was not too unlucky. Not go-live ready on any config tested. `2026-09-12, scripts/validation/revalidate_candidates.py` |

## Rejected and not to be revisited without new evidence

- **"Prioritise live results over backtest results when they disagree."** Recorded in
  `BACKTEST_VS_LIVE_ANALYSIS.md`. A broken instrument does not mean the truth is
  more favourable; it means there is no measurement. This reasoning replaced a
  30-day sample with three trades from a single trending day and re-enabled a
  strategy on that basis. It is the specific step that needs not to recur.
- **Any performance figure produced before 2026-08-31.** All of it came from the
  fixed-±$5, zero-cost engine on Yahoo futures. Not "approximately right" —
  structurally unrelated to the deployed system.
- **Grading a strategy from fewer than ~100 trades.** BollingerBounce graded C on
  1 trade; AsianSweep C on 1; EMACrossover C− on 2. At the measured per-trade
  expectancy, samples that size carry no information.
- **Fading a swept level / failed-breakout reversal on gold M15** (HYP-070,
  and `Range_rejection_wick` / `Failed_breakout_reversal` in the 20-year audit).
  22 mathematical variations, 5 regime eras, real engine — negative per-trade
  expectancy after costs everywhere. The one cell that scored PF 1.48 on
  2022–2026 scored median PF 0.71 across 22 years. New evidence would need to be
  a different instrument or tick-level sweep detection.
